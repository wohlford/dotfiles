# Host definitions
# Add entries below or create additional .conf files in this directory.
# NOTE: this template is tracked in a public repo — real hosts belong in a
# separate gitignored .conf file in this directory.
#
# Legacy device that still needs SHA-1 crypto (re-add with +; the base
# config subtracts these algorithms globally):
#   Host oldswitch
#     KexAlgorithms +diffie-hellman-group14-sha1
#     HostKeyAlgorithms +ssh-rsa
#     MACs +hmac-sha1
